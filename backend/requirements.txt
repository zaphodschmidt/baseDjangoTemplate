# Pin every direct dependency EXACTLY, and bump Django only alongside a DRF
# release that supports it.
#
# Django 6.1 removed `django.utils.cache.cc_delim_re`, which
# `djangorestframework` still imports in `rest_framework/views.py`. An unpinned
# Django meant a routine reinstall resolved to 6.1 and the application stopped
# importing entirely — with nothing in the repo changed. See CLAUDE.md § Incidents.
#
# Linting is NOT here: pre-commit installs its own ruff, so the production
# image does not ship a tool it never runs.

Django==5.1
gunicorn==22.0.0
psycopg2-binary==2.9.9
dj-database-url==2.1.0
python-dotenv==1.0.1
django-environ==0.11.2
djangorestframework==3.15.1
django-filter==24.2
djangorestframework-simplejwt==5.3.1
drf-spectacular==0.29.0
whitenoise==6.11.0
